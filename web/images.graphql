query ImagesQuery($id: String!) {
  user(id: $id) {
    id
    name
    email
    wb
    halfSize
    quality
    lutId
    images {
      cacheFileName
      cachedUrl
      fileName
      fileSize
      id
      mimeType
      originalUrl
      scanTime
      exif
    }
  }
}